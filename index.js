document.onload = initializeShell();
function initializeShell () {
  var wrapper = document.createElement('div');
wrapper.setAttribute('id', 'gameShell');
  document.getElementById('#gameShell').appendChild(document.createElement('p'));
};
